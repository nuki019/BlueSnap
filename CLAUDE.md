# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

???? (BlueSnap) ??? Android ????????????????AI ????????? HTML ???? WebView ????????2026 ???????????? AIGC ?????????

????Kotlin + Jetpack Compose + Material3 + OkHttp + vivo ????? API (OpenAI ????)?

## Build & Run

```bash
# ??????????
./gradlew :app:compileDebugKotlin

# ?? Debug APK
./gradlew assembleDebug

# ?????
./gradlew installDebug
```

**????**????????? `local.properties`??? .gitignore ??????

```properties
sdk.dir=/path/to/your/Android/Sdk
ai.api.key=YOUR_API_KEY_HERE
ai.base.url=https://api-ai.vivo.com.cn/v1
ai.model=Doubao-Seed-2.0-pro
```

API ???? `BuildConfig` ???? `app/build.gradle.kts` ?? `buildConfigField`??????? `BuildConfig.AI_API_KEY` / `BuildConfig.AI_BASE_URL` / `BuildConfig.AI_MODEL`?

## Testing

```bash
# ????
./gradlew testDebugUnitTest

# Instrumented ?????????
./gradlew connectedDebugAndroidTest
```

???????????
- `app/src/test/.../ExampleUnitTest.kt`
- `app/src/androidTest/.../ExampleInstrumentedTest.kt`

## Architecture

**? Activity + Compose Navigation** ???MVVM ??????

### ???

```
???? -> MainViewModel.sendMessage()
         -> AiService.chatStream() (SSE ??)
         -> StateFlow<AppState> ??
         -> Compose UI ????
```

### ????

| ? | ???? | ?? |
|---|---|---|
| UI | `ui/screens/*.kt`, `ui/components/*.kt` | ? Compose UI?????? ViewModel ?? |
| ViewModel | `viewmodel/MainViewModel.kt` | ?? ViewModel??? `AppState` ??? |
| AI ?? | `ai/AiService.kt` (??) | ?? `chat`/`chatStream`/`generatePlan`/`generateHtml` |
| AI ?? | `ai/RealAiService.kt`, `ai/MockAiService.kt` | ?? API / ?? Mock |
| ???? | `data/Models.kt` | `ChatMessage`, `AppPlan`, `GeneratedApp`, `AppState` |

### ???? Jetpack Navigation?

??? Navigation Compose ?????? `AppState.currentScreen` ?? + `AnimatedContent` ???? `ui/navigation/AppNavigation.kt`???????? `viewModel.navigateTo(Screen.XXX)`?

### AI ?????

- `RealAiService`?OkHttp ?? vivo ?? API?SSE ????????????? `data:` ??????????? plan ? HTML ?????? OkHttpClient ??????120s read timeout??????300s??? HTML ????
- `MockAiService`??? 5 ??? HTML ??????/????/???/???/????????????????

`MainViewModel` ?????? `RealAiService()`???? Mock ?????? `MockAiService()`?

### ??????

`chatStream()` ?? `Flow<String>`???? `channelFlow` + `trySend`??ViewModel collect ??? `AppState.streamingContent`?ChatScreen ? `StreamingBubble` ?????????????????????? `ChatMessage`?

**????**?`flow {}` ? `emit()` ? `withContext(Dispatchers.IO)` ???????????? `channelFlow` + `trySend`???????????? "flow exception transparency is violated"?

### ????????

`MainViewModel.shouldAutoPlan()` ????????"??"/"???"/"????"/"????"/"????"??? AI ??????????????? `generatePlan()` ??? PlanScreen?

### WebView ??

`PreviewScreen` ?? `AppWebView` ? `https://localhost` ?? base URL ?? HTML??? localStorage ?????? `DEBUG_JS_BRIDGE` ???? JS ??? Logcat?????????????????????? URL ???

### Prompt ??

?? system prompt ??? `RealAiService.companion` ??
- `CHAT_SYSTEM_PROMPT`??????????????
- `PLAN_SYSTEM_PROMPT`????? JSON ????
- `HTML_SYSTEM_PROMPT`?????? HTML ??????? JS ????

## Key Patterns & Pitfalls

1. **HTML ?????????**?`MockAiService.kt` ?????? HTML ???? Kotlin raw string??????????? `${...}`?? JS ?????????????????????? Kotlin ??????????

2. **API Key ??**?`ai.api.key` ??? `local.properties`?git ignored???? Gradle `buildConfigField` ??? `BuildConfig`???? key ???????

3. **JSON ????**?`RealAiService.parsePlan()` ? `extractHtml()` ???? + ?? JSONObject ?????? fallback?????? -> ? JSON ?? -> ????????????????????

4. **????**?????????? `AppState` data class ???? `MutableStateFlow.update {}` ?????????????????? `AppState` ?????? `MainViewModel` ???????????

5. **Edge-to-edge**?`MainActivity` ??? `enableEdgeToEdge()`?ChatScreen ? PreviewScreen ?????? `imePadding()` + `navigationBarsPadding()` ???

## File Layout

```
app/src/main/java/com/example/bluesnap/
  MainActivity.kt              -- ?? Activity?setContent ??
  data/Models.kt               -- ???????ChatMessage, AppPlan, GeneratedApp, AppState, Screen ???
  ai/AiService.kt              -- AI ?????4 ????
  ai/RealAiService.kt          -- ?? API ?? + prompt ?? + AiException
  ai/MockAiService.kt          -- Mock ?? + 5 ? HTML ???~400 ??
  viewmodel/MainViewModel.kt   -- ???? + ????
  ui/navigation/AppNavigation.kt  -- AnimatedContent ??
  ui/screens/HomeScreen.kt     -- ?????? + ?? chips + ???
  ui/screens/ChatScreen.kt     -- ???????? + ???? + ???
  ui/screens/PlanScreen.kt     -- ????????? + ????
  ui/screens/PreviewScreen.kt  -- WebView ?? + JS ??? + ????
  ui/screens/ShareScreen.kt    -- ????????/?? HTML
  ui/components/MessageBubble.kt   -- ????????
  ui/components/StreamingBubble.kt -- ??????????????
  ui/components/PlanCard.kt        -- ??????
  ui/components/TemplateChip.kt    -- ?????? + defaultTemplates ??
  ui/theme/Color.kt            -- ???????BluePrimary ???
  ui/theme/Theme.kt            -- Material3 ?????/???????????
  ui/theme/Type.kt             -- ????

docs/design.md                 -- ????????
docs/optimization.md           -- ?????????
???/                        -- ??????LaTeX ??? + Markdown?
```

## Code Comments Language

????????????Prompt ???????
